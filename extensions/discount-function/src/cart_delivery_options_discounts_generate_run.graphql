query DeliveryInput {
  cart {
    deliveryGroups {
      id
    }
  }
  discount {
    discountClasses
    metafield(
      namespace: "$app:smart-variant-discounts"
      key: "function-configuration"
    ) {
      value
    }
  }
}
